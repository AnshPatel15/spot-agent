/**
 * Song suggestion & playlist creation agent using LangChain.js ReAct agent.
 * Learning: ReAct = Reason + Act loop. Agent calls tools based on LLM reasoning.
 * Combines LLM + Spotify tools.
 */

import { createReactAgent } from '@langchain/core/agents';
import { pull } from 'langgraph/prebuilt'; // For graph if needed later

// TODO: Create agent with tools and LLM

export { };