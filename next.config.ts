import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: HTTPS configuration for development should be handled externally
  // The server.https option was removed in Next.js 12
};

export default nextConfig;
