/**
 * Interactive chat UI for the agent.
 * Learning: Use React hooks for state, server actions for agent calls.
 */

export default function AgentChat() {
  // TODO: Chat input, messages state, call agent via API route or server action
  return (
    <div>
      <h1>Song Suggestion Agent</h1>
      <p>Chat with the agent to get song recs and create playlists!</p>
    </div>
  );
}